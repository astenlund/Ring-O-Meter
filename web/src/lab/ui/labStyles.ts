import type {CSSProperties} from 'react';

// Shared band container style for the lab's stacked bands.
export const bandStyle: CSSProperties = {padding: 16, background: '#202020', borderRadius: 8, marginBottom: 16};
